pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r index.html newsletter.html css js assets public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
